    <tr id="link-<%= link.id %>">
      <td><%= link.title %></td>
      <td><%= link.url %></td>
      <td data-link-id="<%= link.id %>" class="read-status"><%= link.read %></td>
      <td data-link-id="<%= link.id %>"> <button class="mark-as-read btn">Mark as Read</button></td>
      <td id="edit" data-link-id="<%= link.id %>" ><%= link_to 'Edit', edit_link_path(link), class: 'btn'%></td>
    </tr>
